// This file was automatically generated by genny.
// Any changes will be lost if this file is regenerated.
// see https://github.com/cheekybits/genny

package indexof

import (
	"math/rand"
	"testing"
	"time"
)

func TestI64(t *testing.T) {
	rnd := rand.New(rand.NewSource(time.Now().UnixNano()))
	expect := map[int64]int{}

	var Int64s []int64

	c := 0
	for i := 0; i < 256; i++ {
		v := int64(rnd.Int()) // l o l

		// skip if we've already done this one
		if _, ok := expect[v]; ok {
			continue
		}

		Int64s = append(Int64s, v)
		expect[v] = c
		c++
	}

	for i := range expect {
		got := I64(i, Int64s)
		want := expect[i]

		if got != want {
			t.Errorf("Int64(%d) - expected at position %d, got position %d", i, want, got)
		}
	}
}
